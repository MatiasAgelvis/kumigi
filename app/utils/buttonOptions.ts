import { ButtonProps, useBreakpointValue } from "@chakra-ui/react";

const buttonOptions: ButtonProps = {
  width: "100%",
  colorScheme: "teal",
  variant: "outline",
};

export const buttonSize = { base: "sm", lg: "md" };

export default buttonOptions;
